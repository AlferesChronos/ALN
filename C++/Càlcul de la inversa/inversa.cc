#include <fstream>
#include <vector>
#include <iostream>

#define A "inputA"
#define b "inputb"

int main() {
    int n;
    cin >> n;
    ofstream of;
    of.open(A,ofstream::out);
    of.close();
    of.open(b,ofstream::out);
    of << 1 << endl;
    of << 
    of.close();
}
